# Production-Only Dependencies
# Minimal requirements for production deployment
# Install with: pip install -r requirements-prod.txt

# === CORE DEPENDENCIES ===
kubernetes>=28.0.0,<30.0.0          # Kubernetes Python client
prometheus-api-client>=0.5.3,<0.6.0  # Prometheus API integration

# === RUNTIME UTILITIES ===
PyYAML>=6.0.1,<7.0.0                # YAML parsing for configurations
requests>=2.31.0,<3.0.0             # HTTP client (used by prometheus-api-client)